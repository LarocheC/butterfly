---
phase: 03-strip-and-verify
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - .gitignore
  - README.md
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-04
must_haves:
  truths:
    - "build/ and torch_butterfly.egg-info/ are in .gitignore and not tracked"
    - ".gitignore has no stale entries referencing deleted directories"
    - "README.md reflects Python 3.10+, PyTorch 2.x, uv pip install workflow"
    - "README.md preserves academic paper references"
    - "pytest tests/ passes with all tests green"
  artifacts:
    - path: ".gitignore"
      provides: "Clean ignore file with build artifact patterns, no stale entries"
      contains: "torch_butterfly.egg-info/"
    - path: "README.md"
      provides: "Modernized documentation for torch_butterfly"
      contains: "uv pip install"
  key_links: []
---

<objective>
Clean up .gitignore (remove stale entries, ensure build artifacts ignored), modernize README.md, and verify all tests pass after the full cleanup.

Purpose: Finalize the repository state so it is ready for CI/CD pipeline onboarding with accurate documentation and clean configuration.
Output: Updated .gitignore, modernized README.md, green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strip-and-verify/03-CONTEXT.md
@.planning/phases/03-strip-and-verify/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean .gitignore and modernize README.md</name>
  <files>.gitignore, README.md</files>
  <read_first>
    - .gitignore (current content -- need to see all entries)
    - README.md (current content -- need to see what to preserve vs replace)
    - pyproject.toml (for accurate version constraints to put in README)
  </read_first>
  <action>
**Update .gitignore (per D-01, CLEAN-03):**

Remove these 6 stale lines from the TOP of .gitignore (lines 1-6 before the .DS_Store line):
```
learning_transforms/logs/
learning_transforms/results/
cnn/logs/
cnn/results/
config/
data/
```

Add this line after the existing `*.egg-info/` pattern (which is around line 34):
```
torch_butterfly.egg-info/
```

Note: `build/` is already covered by the existing `build/` entry on line 22. The generic `*.egg-info/` on line 34 covers egg-info too, but add the explicit `torch_butterfly.egg-info/` entry for clarity per CLEAN-03.

Also ensure `torch_butterfly.egg-info/` is not tracked. Run `git rm -r --cached torch_butterfly.egg-info/ 2>/dev/null || true` (it is currently untracked so this is a no-op safety check).

**Rewrite README.md (per D-02):**

Replace the entire README.md with modernized content. The new README MUST:
1. Keep the two paper references from the first paragraph (Learning Fast Algorithms + Kaleidoscope) with their URLs
2. State Python >=3.10 and PyTorch >=2.0 as requirements
3. Show `uv pip install .` as the primary install method, `pip install .` as alternative
4. Show `uv pip install -e ".[dev]"` for development
5. Mention CUDA support is automatic when available, with FORCE_CUDA=1 / FORCE_CPU=1 overrides
6. Keep the usage section pointing to `torch_butterfly/special.py` and `tests/test_special.py`
7. NOT mention: conda, old `butterfly/` interface, `learning_transforms/`, `setup.py install`, any experiment directories
8. Include a brief "What is this?" section explaining butterfly matrices for structured linear transforms

Commit both changes:
Run `git add .gitignore README.md && git commit -m "chore(03): clean .gitignore and modernize README"`
  </action>
  <verify>
    <automated>grep -c "learning_transforms" .gitignore; grep -c "cnn/" .gitignore; grep -c "torch_butterfly.egg-info/" .gitignore; grep -c "uv pip install" README.md; grep -c "conda" README.md; grep -c "old interface" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "learning_transforms" .gitignore` returns no matches (exit code 1)
    - `grep "cnn/" .gitignore` returns no matches (exit code 1)
    - `grep "config/" .gitignore` returns no matches (exit code 1)
    - `grep "^data/$" .gitignore` returns no matches (exit code 1) -- the stale project-specific `data/` line is removed
    - `grep "torch_butterfly.egg-info/" .gitignore` returns a match
    - `grep "build/" .gitignore` returns a match
    - `grep "uv pip install" README.md` returns a match
    - `grep "3.10" README.md` returns a match
    - `grep "conda" README.md` returns no matches (exit code 1)
    - `grep -i "old interface" README.md` returns no matches (exit code 1)
    - `grep "arxiv.org/abs/1903.05895" README.md` returns a match (paper preserved)
    - `grep "Kaleidoscope" README.md` returns a match (paper preserved)
  </acceptance_criteria>
  <done>.gitignore has no stale entries and covers build artifacts; README.md is modernized with current install instructions and preserved paper references</done>
</task>

<task type="auto">
  <name>Task 2: Verify all tests pass (CLEAN-04)</name>
  <files></files>
  <read_first>
    - tests/test_butterfly.py (verify it only imports from torch_butterfly, not removed dirs)
  </read_first>
  <action>
Run the full test suite to confirm nothing was broken by the cleanup:

1. Run `cd /home/clement/butterfly && python -m pytest tests/ -v` and verify all tests pass.

If any test fails:
- Check if the failure is related to removed directories (it should not be -- CONTEXT.md confirms no cross-imports)
- If failure is unrelated to cleanup (e.g., CUDA not available), note it but do not count as a cleanup regression

This task produces no file changes -- it is purely verification for CLEAN-04.
  </action>
  <verify>
    <automated>cd /home/clement/butterfly && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` output contains "passed" and does not contain "FAILED"
    - `python -m pytest tests/ --co -q 2>&1 | tail -1` shows test count (confirms tests are collected)
    - No test imports from butterfly/, learning_transforms/, or any removed directory: `grep -r "from butterfly\|from learning_transforms\|from cnn\|from convolution" tests/` returns no matches
  </acceptance_criteria>
  <done>All tests in tests/ pass, confirming the cleanup introduced no regressions (CLEAN-04)</done>
</task>

</tasks>

<verification>
Full phase verification after both plans complete:
1. `ls butterfly/ tests_old/ learning_transforms/ fairseq/ cnn/ convolution/ transformer/ gumbel-sinkhorn/ data/ .gitmodules ray_template.sh 2>&1 | grep -c "No such file"` equals 11
2. `grep -c "learning_transforms\|cnn/results\|cnn/logs\|^config/\|^data/" .gitignore` equals 0
3. `grep "torch_butterfly.egg-info/" .gitignore` matches
4. `grep "uv pip install" README.md` matches
5. `python -m pytest tests/ -v` all green
</verification>

<success_criteria>
- .gitignore cleaned of all stale project-specific entries
- build/ and torch_butterfly.egg-info/ covered in .gitignore
- README.md reflects Python 3.10+, PyTorch 2.x, uv/pip install workflow
- README.md preserves paper references, removes all old interface / experiment mentions
- All tests pass after full cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/03-strip-and-verify/03-02-SUMMARY.md`
</output>
