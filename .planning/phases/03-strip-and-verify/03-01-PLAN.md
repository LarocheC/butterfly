---
phase: 03-strip-and-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - butterfly/
  - tests_old/
  - learning_transforms/
  - cnn/
  - convolution/
  - transformer/
  - gumbel-sinkhorn/
  - data/
  - fairseq/
  - .gitmodules
  - ray_template.sh
autonomous: true
requirements:
  - LEGACY-01
  - LEGACY-02
  - LEGACY-03
  - LEGACY-04
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - CLEAN-01
  - CLEAN-02
must_haves:
  truths:
    - "The directories butterfly/, tests_old/, learning_transforms/, cnn/, convolution/, transformer/, gumbel-sinkhorn/, data/ do not exist"
    - "The fairseq/ submodule is fully removed including .gitmodules"
    - "ray_template.sh does not exist"
  artifacts:
    - path: "butterfly/"
      provides: "Removed — must NOT exist"
    - path: "tests_old/"
      provides: "Removed — must NOT exist"
    - path: "learning_transforms/"
      provides: "Removed — must NOT exist"
    - path: "cnn/"
      provides: "Removed — must NOT exist"
    - path: "convolution/"
      provides: "Removed — must NOT exist"
    - path: "transformer/"
      provides: "Removed — must NOT exist"
    - path: "gumbel-sinkhorn/"
      provides: "Removed — must NOT exist"
    - path: "data/"
      provides: "Removed — must NOT exist"
    - path: "fairseq/"
      provides: "Removed — must NOT exist"
    - path: ".gitmodules"
      provides: "Removed — must NOT exist"
    - path: "ray_template.sh"
      provides: "Removed — must NOT exist"
  key_links: []
---

<objective>
Remove all legacy code, experiment directories, dead assets, and the fairseq submodule from the repository.

Purpose: Strip the repo down to only the core torch_butterfly library, build files, and tests in preparation for CI/CD pipeline onboarding.
Output: A clean repo with only torch_butterfly/, csrc/, tests/, build config, and repo essentials remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strip-and-verify/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy directories and the fairseq submodule</name>
  <files>butterfly/, tests_old/, learning_transforms/, fairseq/, .gitmodules</files>
  <read_first>
    - .gitmodules (current submodule definition)
  </read_first>
  <action>
Remove the fairseq git submodule properly (per D-03):
1. Run `git rm fairseq` to unstage the submodule
2. Run `rm -rf .git/modules/fairseq` to clean cached submodule data
3. Run `git rm .gitmodules` to remove the submodule config file

Remove legacy directories via git:
4. Run `git rm -rf butterfly/` (LEGACY-01: old butterfly implementation)
5. Run `git rm -rf tests_old/` (LEGACY-02: old tests)
6. Run `git rm -rf learning_transforms/` (LEGACY-03: Cython experiments)

All removals are `git rm` so they are staged for commit. Do NOT run `git commit` yet -- Task 2 will add more removals before committing.
  </action>
  <verify>
    <automated>test ! -d butterfly && test ! -d tests_old && test ! -d learning_transforms && test ! -d fairseq && test ! -f .gitmodules && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `ls butterfly/ 2>&1` outputs "No such file or directory"
    - `ls tests_old/ 2>&1` outputs "No such file or directory"
    - `ls learning_transforms/ 2>&1` outputs "No such file or directory"
    - `ls fairseq/ 2>&1` outputs "No such file or directory"
    - `ls .gitmodules 2>&1` outputs "No such file or directory"
    - `ls .git/modules/fairseq 2>&1` outputs "No such file or directory"
    - `git status` shows all removals staged
  </acceptance_criteria>
  <done>butterfly/, tests_old/, learning_transforms/, fairseq/, and .gitmodules are fully removed from working tree and git index</done>
</task>

<task type="auto">
  <name>Task 2: Remove experiment directories and dead files</name>
  <files>cnn/, convolution/, transformer/, gumbel-sinkhorn/, data/, ray_template.sh</files>
  <read_first></read_first>
  <action>
Remove experiment directories via git:
1. Run `git rm -rf cnn/` (EXP-01)
2. Run `git rm -rf convolution/` (EXP-02)
3. Run `git rm -rf transformer/` (EXP-03)
4. Run `git rm -rf gumbel-sinkhorn/` (EXP-04)

Remove dead assets:
5. Run `git rm -rf data/` (CLEAN-01)
6. Run `git rm ray_template.sh` (CLEAN-02)

Commit all removals from Task 1 and Task 2 together:
7. Run `git commit -m "chore(03): remove legacy code, experiments, and dead assets"` to commit all staged removals.
  </action>
  <verify>
    <automated>test ! -d cnn && test ! -d convolution && test ! -d transformer && test ! -d gumbel-sinkhorn && test ! -d data && test ! -f ray_template.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `ls cnn/ 2>&1` outputs "No such file or directory"
    - `ls convolution/ 2>&1` outputs "No such file or directory"
    - `ls transformer/ 2>&1` outputs "No such file or directory"
    - `ls gumbel-sinkhorn/ 2>&1` outputs "No such file or directory"
    - `ls data/ 2>&1` outputs "No such file or directory"
    - `ls ray_template.sh 2>&1` outputs "No such file or directory"
    - `git log --oneline -1` shows "chore(03): remove legacy code, experiments, and dead assets"
  </acceptance_criteria>
  <done>cnn/, convolution/, transformer/, gumbel-sinkhorn/, data/, and ray_template.sh are removed and committed</done>
</task>

</tasks>

<verification>
All 11 directories/files removed from working tree and git history:
- `ls butterfly/ tests_old/ learning_transforms/ fairseq/ cnn/ convolution/ transformer/ gumbel-sinkhorn/ data/ ray_template.sh .gitmodules 2>&1 | grep -c "No such file"` returns 11
- `git status` shows clean working tree after commit
</verification>

<success_criteria>
- Zero legacy or experiment directories remain in the repo
- fairseq submodule fully deregistered (no .gitmodules, no .git/modules/fairseq)
- All removals committed in a single clean commit
</success_criteria>

<output>
After completion, create `.planning/phases/03-strip-and-verify/03-01-SUMMARY.md`
</output>
